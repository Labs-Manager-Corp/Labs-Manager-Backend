spring.application.name=Lab Manager API

project.version=@project.version@
spring.profiles.active=@spring.profiles.active@
spring.main.allow-bean-definition-overriding=true

server.port = 8000

server.servlet.contextPath=/lab-api
server.error.include-message=always
server.error.include-stacktrace=never


# Springdoc OpenAPI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Swagger config
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha

# # Configuração do DataSource
# # Maximum number of milliseconds that a client will wait for a connection
# spring.datasource.hikari.connection-timeout=20000
# # Minimum number of idle connections maintained by HikariCP in a connection pool
# spring.datasource.hikari.minimum-idle=10 
# # Maximum pool size
# spring.datasource.hikari.maximum-pool-size=30
# spring.datasource.driver-class-name:org.postgresql.Driver
# spring.datasource.url=jdbc:postgresql://${POSTGRES_COILID_HOST}:${POSTGRES_COILID_PORT}/${POSTGRES_COILID_SERVICE}
# spring.datasource.username=${POSTGRES_COILID_USER}
# spring.datasource.password=${POSTGRES_COILID_PASSWORD}
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# spring.security.oauth2.resourceserver.jwt.issuer-uri: ${AUTH_REALMS_WEG_NET}
spring.security.oauth2.resourceserver.jwt.audiences: labs-manager-data-provider
